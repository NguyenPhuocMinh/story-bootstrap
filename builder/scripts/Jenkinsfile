pipeline {
    agent any
    stages {
        stage('Clone') {
            steps {
                git 'https://github.com/NguyenPhuocMinh/story-bootstrap.git'
            }
        }
        // stage('Install') {
        //     steps {
        //         sh 'npm install'
        //         sh 'npm run bootstrap'
        //         sh 'npm run lint'
        //     }
        // }
        // stage('Build') {
        //     steps {
        //         sh 'npm run build'
        //     }
        // }
        // stage('Test') {
        //     steps {
        //         sh 'npm test'
        //     }
        //     post {
        //         always {
        //             junit 'junit.xml'
        //         }
        //     }
        // }
    }
}
