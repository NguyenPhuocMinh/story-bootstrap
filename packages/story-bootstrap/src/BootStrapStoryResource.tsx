import { ResourceCore, ResourceCoreProps } from 'sb-core';

const BootStrapStoryResource = (props: ResourceCoreProps) => (
  <ResourceCore {...props} />
);

BootStrapStoryResource.displayName = 'BootStrapStoryResource';

export default BootStrapStoryResource;
